# Tools

Development tools that are not exported as game content live here.

Tools can use their own runtime, package manager, and local configuration. Keep generated dependencies and secrets ignored, and document setup inside each tool folder.

Current tools:

- `server/`: local Node.js dialog backend helper.
